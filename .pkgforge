name 'libcap'
org 'amylum'

licenses 'License'

deps(
  pam: '1.3.0-21',
  'linux-api-headers': '4.9.5-1'
)

cflags
harden

build do
  run 'sed -i "/SBINDIR/s#sbin#bin#" Make.Rules'
  run "make CC=musl-gcc CFLAGS='#{@forge.cflags.join(' ')}'"
  install 'prefix' => '/usr', 'lib' => '/lib'
end

test do
  # TODO: add tests
end
